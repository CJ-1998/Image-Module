eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL}
    register-with-eureka: true
    fetch-registry: true

management:
  zipkin:
    tracing:
      endpoint: ${ZIPKIN_BASE_URL}
